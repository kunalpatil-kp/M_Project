// utils/menuGenerator.js
// AI Event Ordering — boilerplate only, no business logic yet.

/**
 * Build a menu list from available food items filtered by event criteria.
 * @param {Array}  foodItems  - all available food items from DB
 * @param {Object} criteria   - { eventType, guestCount, budget }
 * @returns {Array} filtered and sorted menu items
 */
const generateMenu = (foodItems, criteria) => {
  // TODO: implement menu generation logic
  return [];
};

export { generateMenu };
